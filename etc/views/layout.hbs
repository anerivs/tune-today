<!DOCTYPE html>

    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/site.css" type="text/css" title="no title" charset="utf-8">
    </head>
    <body>
        <div id="outer">
            <header>
                <nav> <!--navbar-->
                    <span class="home-link"><a href="/">home</a> {{#if user}} | <a href="/song-add">add song!</a>{{/if}}</span>
                    <span class="login-status">
                        {{#if user}}
                            welcome, {{user.username}}!
                        {{else}}
                            <a href="/register">register</a> or <a href="/login">login</a>
                        {{/if}}
                    </span>
                </nav>
            </header>
            <h1>welcome to tunetoday!</h1>
            <div class="content"> <!--body content-->
                {{{body}}}
            </div>
        </div>
    </body>
</html>